apiVersion: apps/v1
kind: Deployment
metadata: 
   name: actions-deployment

spec:
  replicas: 2
  selector:
    app: actions
  
  template:
      metadata:
        labels:
          app: actions
      spec:
         containers:
           -name: actions-conatiner
           image: actions
           ports:
           -containerPort: 3000